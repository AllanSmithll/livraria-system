export default interface Livro {
        id: string;
        title: string;
        resume: string
        gender: string;
        pages: number;
        date: string;
        edition: number;
        nameAuthor: string;
        namePublisher: string;
}